<Page x:Class="EstoqueLoja.WPF.Views.ProdutosPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="White">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Produtos"
                   FontSize="26"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,15">

            <TextBox x:Name="TxtBuscaNome"
                     Width="250"
                     Height="35"
                     Margin="0,0,10,0"/>

            <TextBox x:Name="TxtBuscaRef"
                     Width="180"
                     Height="35"
                     Margin="0,0,10,0"/>

            <Button Content="Buscar"
                    Width="100"
                    Height="35"
                    Click="BtnBuscar_Click"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  x:Name="GridProdutos"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  VerticalAlignment="Stretch"
                  HorizontalAlignment="Stretch">

            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Ref" Binding="{Binding Ref}" Width="120"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="120"/>
                <DataGridTextColumn Header="Compra" Binding="{Binding Valor_Compra}" Width="120"/>
                <DataGridTextColumn Header="Venda" Binding="{Binding Valor_Venda}" Width="120"/>
            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</Page>